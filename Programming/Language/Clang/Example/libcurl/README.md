# [libcurl](https://curl.se/) - the multiprotocol file transfer library

```bash
apt install libcurl4-openssl-dev
apt install libcurl4-gnutls-dev
```

```bash
gcc -Wall -O3 -o <exec> <src> -DDEBUG=1 -lcurl
```
